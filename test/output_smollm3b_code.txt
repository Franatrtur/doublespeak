import os

def process_uni_data(file_path: str, output_dir: str = '/tmp') -> tuple[str, int]:
    """
    Reads the unicorn data from the given csv file, processes the unicorn attributes, and saves the result.

    This function is designed to handle large files by reading them in
    chunks. It will return the most important line (including line
    number), along with the line number, so that the user can decide
    whether to continue processing the file.

    :param file_path: The path to the unicorn data file.
    :param output_dir: The directory to save the processed data to.
    :return: The most important line (including line number), along with the line number.
    """
